import UIKit
import Domain

class BoardCardView:BoardTextView {
    weak var card:Card!
    
    func dragBegin(point:CGPoint) {
        
    }
    
    func dragUpdate(point:CGPoint) {
        
    }
}
